name: run CodeStyle

on: [push]

jobs:
  run-phpunit-tests:
    runs-on: ubuntu-latest
    steps:
      - name: composer install
        run: |
          php composer-setup.php --install-dir=bin --filename=composer
          composer install
        
     
  
